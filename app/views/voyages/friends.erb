<div>
<h1>Your friends on the <%= @ship_name %></h1>   
  <section id="timeline">
    <div class="container-fluid wow fadeInRight" data-wow-duration="1s">
      <div id="timeline-slider" class="owl-carousel">

        <% @friends_list.each do |c| %>
          <div class="item">
              <div class="photo"><img src="<%= c.gravatar_url %>" alt="Lector1" class="img-circle"></div>
              
              <div class="desc">
              
                <h3><%= c.full_name %></h3>
                <p><%= c.email %></p>
                <p><%= c.nationality %></p>
                <p><%= @ship_name%></p>
              </div>
          </div>
        <% end %>
      </div>
    </div>
    
  </section>
</div>
